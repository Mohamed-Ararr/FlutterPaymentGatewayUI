import "package:flutter/material.dart";

import "SubWidgets/CartViewBody.dart";

class CartView extends StatelessWidget {
  const CartView({super.key});

  @override
  Widget build(BuildContext context) {
    return const CartViewBody();
  }
}
